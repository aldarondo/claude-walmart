{
  "name": "claude-walmart",
  "version": "1.0.0",
  "description": "MCP server wrapping the Walmart Open API — product search and price lookup. No cart management available via API.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "serve": "node src/serve.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js tests/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^4.21.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "jest": "^30.3.0"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {}
  }
}
